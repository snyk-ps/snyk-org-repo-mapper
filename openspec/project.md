# bitbucket-org-repo-mapper

Python tooling that maps Bitbucket Server repositories to AppSec metadata (YAML / spreadsheet) and emits companion JSON for Snyk workflows (`snyk-orgs.json`, `snyk-import.json`).

See repository [README.md](../README.md) for usage. OpenSpec changes live under `openspec/changes/`.
